// @flow
export type FadeType = {
  top: boolean,
  right: boolean,
  bottom: boolean,
  left: boolean,
}

export type View = {
  width: number,
  height: number,
}

export type Content = {
  width: number,
  height: number,
}

export type Direction = 'both' | 'horizontal' | 'vertical'
